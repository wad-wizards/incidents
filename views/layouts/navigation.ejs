<nav>
    <ul>
      <% if (user == null)  { %>        
          <li><a href="/users/sign-up"
            <% if (locals.title=="Sign Up") { %> class="active" <% } %>
            >Sign Up</a>
          </li>
          <li><a href="/users/login"
            <% if (locals.title=="Login") { %> class="active" <% } %>
            >Login</a></li>
      <% } else { %> 
          <li><a href="/"
            <% if (locals.title=="Incidents") { %> class="active" <% } %>
            >View Incidents</a></li>   
          <li><a href="/users/edit-profile"
            <% if (locals.title=="Edit Profile") { %> class="active" <% } %>
            >Edit Profile</a></li>         
          <li><a href="/users/logout">Logout</a></li>
       <% } %>
  </ul>
</nav>